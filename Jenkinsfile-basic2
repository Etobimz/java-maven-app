@Library('jenkins-shared-library') _

pipeline {
    agent any
    stages {
        stage('Test Shared Lib') {
            steps {
                script {
                    buildJar()
                }
            }
        }
    }
}
