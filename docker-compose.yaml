version: '3.8'
services:
  java-maven-app:
    image: ${IMAGE_NAME}:${VERSION}
    ports:
      - 8077:8080

  postgres:
    image: postgres:13
    ports:
      - 5432:5432
    environment:
      - POSTGRES_PASSWORD=password
